# Dauntless Game Version
# IMPORTANT: Don't forget to update both strings
dauntless_version: OB 0.5.14
patchnotes_version_string: ob-0-5-14

# build_time is added automatically
# it's updated every time the website/data is rebuilt
# which doesn't necessarily mean that the data has changed
build_time: This gets updated when the build script runs
